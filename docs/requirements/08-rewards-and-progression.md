# 09 — Rewards & Progression (Hole Advance, Treasure Rewards, Upgrades, Reroll)

- **ID:** 08-rewards-and-progression
- **Supersedes:** REQ-009 (win), REQ-014 (hole progression), REQ-021, REQ-022, REQ-023, REQ-025
- **Type:** Functional + UI
- **References:** `05-input-and-states.md` (attempts, GAME_OVER, banners), `06-wind-system.md` (supply), `04-physics-and-collision.md` (win check, treasure hit), `07-level-generation.md` (treasure placement), `03-rendering.md` (treasure rendering)

## 0. Tactical Rework Amendment (2026-09-21, normative — shop/coins/loadout removed, passive slots re-added)

- **Reward goes to an empty golfbag slot for spatial, or increments passive stack.** `claimReward(type)` grants via `grantRewardToBag`: spatial `+1` slot (one item per slot), `fieldExtender`/`rangeModifier(powerCell)` `+1` to passive stack each (**+20%** area/strength, see `06` §0), `freeShot` = **+3 to passive freeShot stack** (`Free Shots`). Passive rewards are stackable with `xN` badge, not one per slot limit.
- **Bag full (spatial only):** claiming a spatial when all 4 spatial slots occupied sets `pendingRewardType` and keeps the menu open with title `Bag full — destroy one to take X`. No in-overlay bag cards are shown (`#reward-bag-row` shall not exist). The player discards via the spatial golfbag hotbar underneath: occupied spatial slots get `.discard-target` dotted highlight, plus hint and `Skip (take nothing) [Esc]`. Clicking a spatial bag slot calls `discardBagSlotAndClaimReward(i)` (destroys that item then grants). Hotkeys `1-4` discard, `Esc`/`S`/Skip closes without reward. Passive grants never trigger bag-full (stackable).
- **Owned-only fix removed** — Shop/coins removed, all items unlocked from start per `10-progression.md`. **All reward screens offer exactly 3 options from the full pool** `['magnifier','liquifier','deflector','rotator','fieldExtender','powerCell','freeShot']` (`getSeededRewardOffer` `shuffled.slice(0,3)`) **and are always re-rollable once** (`isRerollDisabled` only checks `rewardRerolled`).
- Reward hints updated: spatial `+1 to bag`, `freeShot` `+3 Free Shots`, `fieldExtender` `+20% area`, `rangeModifier(powerCell)` `+20% strength`. See `10-progression.md` for starting overlay `Choose 2 starting items` (4 spatial, no reroll, 2 picks).

## 1. Hole & Win Definition

- Hole `hole={x,y,radius:14}` (12-16 tunable). Position per `07-level-generation.md`.
- **Win check every tick:** `hypot(ball.pos-hole) < hole.radius + BALL_RADIUS` (edge grazing counts).
  - On **non-final hole** win: auto-advance immediately — `vel=0`, `isMoving=false`, `modifiers` cleared **without extra supply decrement** (already consumed on placement per `06-wind-system.md` §8), `currentHoleIndex++`, `holeAttempts=0` (keep `totalAttempts`), `isFreeShotActive=false`, regenerate `treasure` uncollected, `loadLevel(next)`, `gameState='AIMING'`, `saveProgress()`. No `WIN` overlay, no `Next` button. HUD `Hole: N/M` updates.
  - On **final hole** win: `vel=0`, `isMoving=false`, `gameState='WIN'`, Victory overlay per `03-rendering.md` §4 (`★★★` + `Course Completed!` + `Total: Y` + green `Continue`) → `clearProgress()` → entry menu. Victory only for last hole.

## 2. Hole Advancement

- **Non-final:** auto-advance on hole entry → `modifiers` cleared **without refund** (supply already decremented on placement, so no extra `supply--`), `freeShot` not consumed on win, then `currentHoleIndex++`, `holeAttempts=0` (keep `totalAttempts`), `isFreeShotActive=false`, regenerate `treasure` uncollected, `loadLevel(next)`, `AIMING`. Legacy `Next` not used for intermediate holes.
- **Final:** same clearing without extra consumption (then moot — `clearProgress()` resets supply to `{1,1,1,1,0}`), `clearProgress()` + `mainMenuVisible=true` keep `COURSES_KEY`/`bestTotal` intact; `bestTotal` updated only here (see `09-persistence-and-campaign.md` §5).

## 3. Reward Triggers — Hole-Start (except hole 1) + Treasure Hit

- **State:** `rewardPending`, `rewardOffered[3]`, `rewardRerolled`, `rewardMenuVisible`, `treasure` per hole (see `07-level-generation.md` §4).
- **On new game:** `rewardPending=false`, `rewardMenuVisible=false`, `rewardOffered=[]`, `rewardRerolled=false`, `maxAttempts=10`, `holeAttempts=0`, `supply={1,1,1,1,0}` (`{magnifier:1,liquifier:1,deflector:1,rotator:1,freeShot:0}` legacy `{amplify,nullify,flip,rotate}`), `isFreeShotActive=false`, `treasure` uncollected for hole 1, no reward menu before first attempt. Player starts with one `rotator` (legacy `rotate`).
- **On each subsequent hole** (`currentHoleIndex>0`): `rewardPending=true`, `holeAttempts=0`, `treasure` regenerated uncollected → `showHoleBanner()` `1000ms` (see `05-input-and-states.md` §6) then auto `maybeShowRewardMenu()` shows `Choose an Upgrade` before first attempt. `rewardRerolled` reset to `false`. Hole 1 shows `Hole 1` banner `1000ms` then `AIMING` (no reward).
- **Treasure trigger:** each hole exactly one `treasure` near a tree (see `07-level-generation.md` §4). When `hypot(ball.pos-treasure) < BALL_RADIUS + treasure.radius` and `!isCollected`, collect: `isCollected=true`, `rewardPending=true`, `maybeShowRewardMenu()` shows **immediately** even in `FLYING` (freezes physics). If `rewardMenuVisible`/`WIN`/`GAME_OVER`/`pause`/`mainMenuVisible` active, pending waits until cleared. Collecting does not cost attempt, once per hole, does not block/bounce.
- **Attempts reference:** launch branching is canonical in `05-input-and-states.md` §4 (auto-arm free shot, counted vs free). This file does not re-define it; reward triggers are hole-start and treasure hit only — never shot count. See `05-input-and-states.md` §4 for `attemptsLeft`/`freeShot` persistence and `§5` for `GAME_OVER` immediate on last-attempt failure.

## 4. Max Attempts — Canonical in `05-input-and-states.md`

- `maxAttempts: integer ≥10`, static `10` (no reward increases). Stored for compat, `window.__getMaxAttempts` returns `10`. If legacy saves contain `>10`, clamped but never increased in-game. See `05-input-and-states.md` §4 for `attemptsLeft` derivation and `GAME_OVER` logic.

## 5. Reward Menu — Random 3-of-6 (Field Extender / Power Cell Combined) with HTML Overlay

- Pool `POOL=['magnifier','liquifier','deflector','rotator','freeShot','fieldExtender','powerCell'] (legacy `['amplify','nullify','flip','rotate',...]` mapped)` (7 types but **Field Extender and Power Cell share one slot**; `rotator` red `↻` `#e74c3c` (legacy `rotate`), `freeShot` gold `★` `#f1c40f` `Supply +3`, `fieldExtender` gray `◯` `#808080` `+15% area` (renamed from `areaUp` `+20%`→`+10%`→**+15%**), `powerCell` gray `⚡` `#808080` `+15% strength` (new), legacy `areaUp` alias kept for compat `areaUp` ≡ `fieldExtender`). Per trigger pick **3 distinct** without replacement via seeded `mulberry32` using `campaignSeed+rewardSeedCounter` per `09-persistence-and-campaign.md` §4, not `Math.random`, same pool for reroll, but **Field Extender and Power Cell together have the same probability as any other single item and never appear together**: the offering logic treats them as a **combined slot** (effective pool of 6: 5 base + 1 combined special). If the combined slot is selected, it deterministically resolves to `fieldExtender` or `powerCell` with 50/50 (seeded from `campaignSeed+counter`), guaranteeing at most one of the two per offer and that together they appear in `-43%` of offers (same as each of `amplify`/`nullify`/`flip`/`rotate`/`freeShot` when picking 3-of-6).
- **Pool by course:** on the **3-hole and 6-hole courses** (`activeCourse.holeCount===3||6`) rewards shall only consist of the four placeable field modifiers — the effective pool is `['magnifier','liquifier','deflector','rotator']` and each offer is 3 distinct of those 4 (seeded shuffle, same counter scheme; rerolls use the same restricted pool). On 9-hole and 18-hole courses the full 3-of-6 pool above applies. Tutorial-run single-liquifier offers (§5b) take precedence on the tutorial 3-hole run.
- State per menu `rewardOffered` (current 1–3 after owned-filtering — see §5c; 1 in tutorial runs — see §5b), `rewardRerolled=false` when freshly shown, `rewardMenuVisible` blocks input. Triggers are hole-start and treasure hit; 3-of-7 selection identical (1-of-1 in tutorial).

## 5c. Owned-Only Rewards — Offer Only What You Own (ceil(unique/2), max 3)

- Every reward offer (hole-start and treasure, initial and rerolled) is filtered to item kinds the player owns in personal storage: `rewardOffered = seededOffer.filter(t => (personalSupply[t] ?? 0) > 0)`, preserving seeded order. Backfilling comes from the shuffled candidate order (not just the sliced triple).
- Card count is `min(3, ceil(uniqueKinds / 2))` where `uniqueKinds = countOwnedKinds()` (distinct `personalSupply` keys with count `>0`): 1 unique → 1 card, 2 uniques → 1 card (e.g. Liquifier + Deflector → 1 card), 3-4 uniques → 2 cards, 5+ uniques → 3 cards (max 3 regardless of owned count). `getRewardCardCount()` implements this; `getSeededRewardOffer()`/`getSeededRerollOffer()` slice the seeded owned-pool shuffle to that count (capped by available pool size).
- Fresh installs own only `liquifier:1`, so early rewards are single Liquifier cards. `personalSupply` can never be all-zero (`loadProgression` restores the Liquifier default), so the filtered offer is never empty; should it ever be, the unfiltered seeded triple is used as fallback.
- The overlay supports 1–3 cards (`rewardMenuVisible && 1 <= rewardOffered.length <= 3`); saved offers of length 1–3 restore. `claimReward` validation is unchanged (type must be in the offer).

## 5b. Tutorial Run — Liquifier-Only Single-Card Rewards, No Re-roll

- The run that starts from the loadout shown directly after the `intro-3-hole` cutscene chain for the 3-hole course (`loadoutHideShopDueToIntro===true` at `startCourseWithLoadout`) is the **tutorial run** (`isTutorialRun===true`, persisted in `STORAGE_KEY`, cleared on run end/new course). It covers the whole 3-hole run, not just hole 1.
- During a tutorial run **every** reward trigger (hole-start for holes 2..N and every treasure hit) offers **only liquifiers**: `rewardOffered=['liquifier']` (single card), regardless of seeded pool. `getSeededRewardOffer()`/`getSeededRerollOffer()` return `['liquifier']` when `isTutorialRun`.
- Each tutorial reward overlay shows **exactly one reward card** (`#reward-buttons` has 1 `.reward-button[data-type="liquifier"]`), selectable via click or `1` (keys `2`/`3` no-op). Layout stays centered with the same card style.
- **Re-roll is disabled for the whole tutorial run**: `isRerollDisabled()===true`, `rerollReward()` returns `false`, `#reward-reroll-button` is `disabled` (`disabled===true`, `.disabled`, `opacity 0.06/0.35`, `cursor not-allowed`), and `R`/click does nothing (no attempt cost, no Game Over).
- **HTML overlay** `#reward-overlay` when `rewardMenuVisible`: dim `rgba(0,0,0,0.55)` full-container, title `Choose an Upgrade` 22px `700` white `stroke 5px` centered, three `150×195` HTML buttons centered (`520×360` gap 18, enlarged even more to leave gap between icon and text) inside `#game-container` layered above canvas (`z-index:10`):
  - `magnifier` orange `#e67e22` (icon `img/magnifier-icon.png` `88×88` centered, legacy `amplify-icon.png`), `liquifier` blue `#3498db` (`liquifier-icon.png` `88×88`, legacy `nullify-icon.png`), `deflector` purple `#9b59b6` (`deflector-icon.png` `88×88`, legacy `flip-icon.png`), `rotator` red `#e74c3c` (`rotator-icon.png` `88×88`, legacy `rotate-icon.png`) hint `+1 to supply`, `freeShot` gold `★` `#f1c40f` hint `Supply +3` (text ★ kept, `36px`), `fieldExtender` gray `#808080` (`field-extender-icon.png` `88×88`) hint `+15% area` (was `+20%`→`+10%`→**+15%**), `powerCell` gray `#808080` (`power-cell-icon.png` `88×88`) hint `+15% strength`. **Icon background removed** — no blueish gray gradient behind reward icons (direct on button). Gap between icon bottom and label is `20px`. Button background `fill`/`border` keeps color identity (`rgba(230,126,34,0.28)` etc.). Reward menu icons are `<img>` `88×88` with fallback to text `»/∅/⇄/↻/◯/⚡`; field circles (`drawModifiers`) show no center icons (see `06-wind-system.md` §7) — reward/hotbar use images, field uses edge color + arrows; hotbar icons are `32×32`. All labels white with stroke `4px`; no `Total Attempts: N` subtitle. Legacy `areaUp` label `Area +20%` still renders as `Field Extender +15%` if legacy offer contains `areaUp`.
- **Blocking:** while visible, aim/charge/launch/modifier `1-4` placement ignored; only `1`/`2`/`3` by offered order or click selects; hotbar visible but disabled; gold glow hidden.
- **Selection** (once per menu): click or `1`/`2`/`3` → `magnifier`/`liquifier`/`deflector`/`rotator` `supply[t]++` (legacy `amplify`→`magnifier` etc; order `1 Liquifier,2 Deflector,3 Rotator,4 Magnifier` for hotbar, but reward `1`/`2`/`3` selects offered order), `freeShot` `supply.freeShot+=3` (passive `Attempts Left: X (+Y)`), `fieldExtender`/`areaUp` `fieldExtenderCount+=1` (`areaUpgradeCount` alias) (`areaMultiplier=1+0.15*fieldExtenderCount`, retroactively grows spatial via `getEffectiveModifierRadius()`, see `06-wind-system.md` §6; base `54` → `62.1/70.2/...`), `powerCell` `powerCellCount+=1` (`powerMultiplier=1+0.15*powerCellCount`, `BASE_STRENGTH=5` → `5.75/6.5/...` retroactively strengthens Magnifier/Deflector/Rotator (legacy Amplify/Flip/Rotate) per `06-wind-system.md` §7.1), then `rewardPending=false; rewardMenuVisible=false; rewardOffered=[]`. Save via `saveProgress()`. For treasure menus, `isCollected` remains `true`. `fieldExtender` and `powerCell` are **passive modifiers** — no placement, shown only in hotbar.

## 6. Re-roll — Once per Menu for 1 Counted Attempt (never free)

- While `rewardMenuVisible && !rewardRerolled`, Re-roll button `190×30` centered at `cardY+192` (below cards) shown: available `rgba(255,255,255,0.12)` border `1.5px` `↻ Re-roll (1 attempt) [R]`; when `rerolled` disabled `0.06/0.35` `not-allowed`; **when on last attempt (`getAttemptsLeft()<=1` or `isLastAttemptForReset()` — `attemptsLeft===1 && freeShot===0`) the button is disabled (`disabled`/`.disabled`, `getComputedStyle` `opacity 0.06/0.35`, `cursor not-allowed`, `disabled===true`) and `R`/click does nothing — you cannot kill yourself with a re-roll.** **During tutorial runs (`isTutorialRun`, see §5b) the button is likewise always disabled and `rerollReward()` always returns `false`.** **Re-roll is also disabled while the player owns fewer than two different kinds of items** (`countOwnedKinds() < 2` where kinds are the 7 `personalSupply` keys with count `>0`): `isRerollDisabled()===true`, `rerollReward()` returns `false`, button `disabled`, `R`/click no-ops (no attempt cost, no Game Over). With 2 uniques (1 card) re-roll is enabled; with 5 uniques (3 cards) re-roll is enabled. Hit-test via `getRewardRerollButtonLayout(W,H)` returns disabled rect when rerolled or on last attempt or in tutorial or when owned kinds `<2`.
- Input: click or `R`/`r` while menu visible and not yet rerolled and **not on last attempt** (`getAttemptsLeft()>1`) → `rerollReward()` exactly once (hotkey `R`, not `0`):
  ```
  if (getAttemptsLeft()<=1) return false; // last attempt: reroll disabled, no cost, no Game Over
  holeAttempts+=1; totalAttempts+=1; attemptsLeft = max(0, maxAttempts - holeAttempts); updateAttemptsUI(); saveProgress();
  if (attemptsLeft <=0 && !WIN) { showGameOver(); } else { rewardRerolled=true; rewardOffered=shuffle([...POOL]).slice(0,3); }
  // do NOT touch maxAttempts, do NOT consume freeShot; isFreeShotActive forced false during menu
  ```
  Costs counted attempt (never free). **If on last attempt (`attemptsLeft<=1` before reroll), reroll is disabled and does not cost an attempt and does not trigger Game Over.** Otherwise if `attemptsLeft` becomes `0` after cost (should not happen from last attempt because disabled, but if `attemptsLeft` was 2 and becomes 1, no Game Over), Game Over would show only if reroll from 1 to 0 which is now blocked. Counts toward `maxAttempts`. Second `R`/click no-ops, and `R`/click on last attempt no-ops. After reroll new `rewardOffered` selectable via `1`/`2`/`3` only if still have attempts. `Digit0` does not trigger reroll.
- Re-reroll cleared on next fresh menu trigger; `resetGameAfterWin`/`endRun`/`clearProgress` also clears `rewardRerolled`. Blocked when not `rewardMenuVisible` or when `WIN`/`GAME_OVER`.

## 7. Persistence Interactions & Course Records

- Only full course completion (final-hole `WIN`) updates `bestTotal` per course (`09-persistence-and-campaign.md`); `End Run` and `Game Over` do not.
- Counters and `supply`/`maxAttempts`/`isFreeShotActive`/`fieldExtenderCount` (alias `areaUpgradeCount`) / `powerCellCount` /`reward…`/`treasure`/`modifiers`/`aimAngle` are part of `STORAGE_KEY` payload and re-saved after every launch/claim/reroll/placement/treasure-hit/advance (see `09-persistence-and-campaign.md`). Legacy `bouncyBallCount`/`areaUp` removed (kept for compat). `freeShot` starts `0`, `rotate` starts `1`; legacy saves default `0`/`1`. `fieldExtenderCount` and `powerCellCount` start `0`.

## Acceptance Criteria

- [ ] New game hole 1: no reward menu before first attempt, one uncollected treasure near tree, `maxAttempts=10` `Attempts Left: 10` (no `(+Y)` when `freeShot=0`) and `Attempts Left: 10 (+3)` when `freeShot=3`, `supply.rotator=1` (legacy `rotate`) red, order `1 Liquifier(blue) |2 Deflector(purple) |3 Rotator(red) |4 Magnifier(orange)`.
- [ ] Hole 2..N: `maybeShowRewardMenu()` shows `Choose an Upgrade` before first attempt; hole 1 never shows.
- [ ] Treasure: one per hole near tree, `hypot < BALL_RADIUS+radius` collects `isCollected=true`, `rewardPending=true`, shows immediately even in `FLYING`; second hit no-op.
  - [ ] Reward 3-of-6 grants (9/18-hole courses): `magnifier`/`liquifier`/`deflector`/`rotator` `+1` (legacy `amplify`/`nullify`/`flip`/`rotate`), `freeShot` `+3`, `fieldExtender` (alias `areaUp`) `+15% area` retroactively grows (fieldExtender+powerCell together same probability as others, never shown together; `+15%` not `+10%`/`+20%`), `powerCell` `+15% strength` retroactively strengthens Magnifier/Deflector/Rotator (legacy Amplify/Flip/Rotate) (`BASE_STRENGTH=5` → `5.75` with 1); no `Max Attempts +5`/bouncy; hotbar shows 6 slots `liquifier/deflector/rotator/magnifier` (legacy `amplify/nullify/flip/rotate`) + passive `fieldExtender` gray `xN` + `powerCell` gray `xN` with no hotkey, no `title` tooltips, icons are `img.hotbar-icon-img` `32×32` `src="./img/magnifier-icon.png"` (legacy `amplify-icon.png`) etc. (reward menu uses HTML overlay `#reward-overlay` buttons `150×195` `88×88` images `520×360` gap `18` no icon background, `20px` gap to label, field shows no center icons); `freeShot` keeps `★` text `36px`; never both fieldExtender+powerCell in same offer.
  - [ ] Rewards on **3-hole and 6-hole courses** (except the tutorial single-liquifier run) offer **only the four placeable field modifiers**: every `rewardOffered` triple is 3 distinct of `['magnifier','liquifier','deflector','rotator']` (seeded, deterministic per `campaignSeed+rewardSeedCounter`), never `freeShot`/`fieldExtender`/`powerCell`; rerolls draw from the same restricted pool.
- [ ] Rewards are **owned-only with ceil(unique/2) cards, max 3**: every offer contains only kinds with `personalSupply[type] > 0` (seeded order preserved, backfilled from the shuffled pool); card count is `min(3, ceil(uniqueKinds/2))` — 1 unique → 1 card, 2 uniques (e.g. Liquifier + Deflector) → 1 card, 3-4 uniques → 2 cards, 5+ uniques → 3 cards; saved 1–3 offers restore. No `#reward-bag-row` exists in the reward overlay when bag-full (`querySelector('#reward-bag-row')===null`); discard is via golfbag `.discard-target` dotted highlight + Skip.
- [ ] Re-roll is disabled while owning **fewer than two different kinds** (`countOwnedKinds() < 2`): button `disabled===true` (`opacity 0.06/0.35`, `cursor not-allowed`), `R`/click no-ops with no attempt cost and no Game Over — same as the last-attempt/tutorial disabled states. With 2 uniques (1 card) re-roll is enabled; with 5 uniques (3 cards) re-roll is enabled.
- [ ] Attempts: normal increments `holeAttempts`/`totalAttempts` on attempt reset (failure via OB/water/edge/`R` while `FLYING`, not on `handleLaunch`); free attempt reset does not increment and `supply.freeShot` consumed on launch, persists while supply remains; after counter decreases to `1` with `freeShot>0` show `Free Shot!` banner and turn on `isFreeShotActive`, else when `freeShot===0` show `Last Attempt` banner, hide `Next Attempt`, update softlock text and disable `R` after launch; last-attempt failure immediate `GAME_OVER` after decrement to `0` without returning to `AIMING`; fallback `handleLaunch` while `attemptsLeft<=0` also shows Game Over; treasure hit never increments.
- [ ] Re-roll button once `R`/click increments `holeAttempts`/`totalAttempts` counted (never free); **when on last attempt (`getAttemptsLeft()<=1`) the button is disabled (`disabled===true`, `opacity 0.06/0.35`, `cursor not-allowed`) and `R`/click does nothing — you cannot kill yourself with a re-roll (no `holeAttempts` increment, no Game Over);** otherwise if `attemptsLeft→0` shows Game Over, otherwise replaces offer and disables; second `R` no-ops. `Total` 11 after one reroll after hole-start (when not on last attempt).
- [ ] No white card; dim `0.55` high-contrast buttons; HUD underneath shows `Attempts Left`; treasure gold on `game` canvas.

## File Paths

- `src/main.js:1` (`POOL` 6 with `freeShot`, `reward*` state, `maybeShowRewardMenu`, `claimReward`, `rerollReward`, `handleLaunch` branching, `#reward-overlay` HTML)
- `src/render.js:1` (`drawTreasure` — reward menu now HTML, not canvas; `drawHUD` deprecated)
- `src/windThree.js:1` (golden glow for `isFreeShotActive`)
- `src/levels.js:1` (`treasure` generation), `src/terrain.js:1` (helpers)
